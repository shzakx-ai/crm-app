services:
  crm:
    build: .
    ports:
      - "8000:8000"
    user: "1000:1000"       # run as non-root
    volumes:
      - crm-data:/app/data
    env_file:
      - .env
    environment:
      - CRM_DB=/app/data/crm.db
    restart: unless-stopped

volumes:
  crm-data: